cmake_minimum_required(VERSION 3.30)
file(GLOB assignment_pro CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/*.pro")
list(LENGTH assignment_pro assignment_pro_count)
if(NOT assignment_pro_count EQUAL 1)
    message(FATAL_ERROR "Expected exactly one .pro file in ${CMAKE_CURRENT_SOURCE_DIR}")
endif()
get_filename_component(cs106_target "${assignment_pro}" NAME_WE)
string(REPLACE " " "_" cs106_target "${cs106_target}")
project("${cs106_target}" LANGUAGES CXX)
include("${CMAKE_CURRENT_SOURCE_DIR}/../cmake/CS106Assignment.cmake")
cs106_configure_assignment("${cs106_target}")
